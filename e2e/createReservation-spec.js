/*
describe('rezcaster create reservation', function () {

	it('login form should accept correct credentials', function () {
		browser.get('https://uat.mybookingpal.com/rezcaster/#/login');

		var loginURL = browser.getCurrentUrl();

		element(by.model('credentials.username')).sendKeys('info@blueviews.com');
		element(by.model('credentials.password')).sendKeys('password');
		element(by.css('[type="submit"]')).click();

		//PropertFore2eDONTREMOVE
		browser.waitForAngular();
		browser.get('https://uat.mybookingpal.com/rezcaster/#/home/reservations/add///');

		element(by.model('searchParam')).sendKeys('PropertFore2eDONTREMOV');
		browser.waitForAngular();
		var searchList = element.all(by.repeater('suggestion in suggestions'));

		searchList.get(1).click();

	});


});*/
